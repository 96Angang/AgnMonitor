# ── Python / 가상환경 / 캐시 ──
__pycache__/
*.py[cod]
*$py.class
*.so
.venv/
data/.venv/
env/
venv/
ENV/

# ── 시크릿 / 환경변수 ──
.env
data/.env
.secret_key
data/.secret_key
data/core_dashboard/.secret.key

# ── 런타임 데이터 (대용량, 컨테이너 영구 볼륨) ──
mariadb_data/
valkey_data/
backup/
data/backup/

# ── Django 수집 정적파일 / DB / 미디어 ──
data/staticfiles/
data/db.sqlite3
data/db.sqlite3-journal
data/media/

# ── 로그 ──
data/logs/
*.log

# ── 배포 / 백업 산출물 ──
*.sh
!data/entrypoint.sh
deploy*.tar.gz
release/
*.tar.gz

# ── 문서 산출물 ──
*.docx

# ── 에디터 / OS ──
.claude/
.gemini/
.DS_Store
Thumbs.db
